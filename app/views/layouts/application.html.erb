<!DOCTYPE html>
<html>
<head>
  <title><%= "#{yield(:title)} | " if content_for?(:title) %> Heartful.ly</title>
  <%= stylesheet_link_tag('application', media: 'all') %>
  <%= javascript_include_tag "vendor/modernizr" %>
  <link href='http://fonts.googleapis.com/css?family=Pacifico|Open+Sans:400italic,400,600' rel='stylesheet' type='text/css'>
  <link href="http://maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= csrf_meta_tags %>
</head>
<body>

  <%= render 'shared/header' %>

  <div id="flash">
    <% flash.each do |key, value| %>
      <div class="flash <%= key %>"><%= value %></div>
    <% end %>
  </div>

  <%= yield %>

  <%=
    javascript_include_tag('http://fb.me/react-with-addons-0.11.2.min.js')
    javascript_include_tag('http://fb.me/JSXTransformer-0.11.2.js')
    javascript_include_tag('application')
  %>
</body>
</html>