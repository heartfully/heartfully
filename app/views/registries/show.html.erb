<%= render 'hero'%>
<%= render "shared/breaker" %>
<%= render 'paragraph' %>

<div class="test">
  <%= render "project_tabbed" %>
</div>

<div class="c-breaker c-breaker--small" style="border-top: 1px solid #ddd; border-bottom: 1px solid #ddd;">
  <div class="block center mb--small">
    <svg class="c-icon c-icon--large" title="gifts"><use xlink:href="#icon-gift"></use></svg>
    <h4 class="meta center mb--xl">Registry Gift Items</h4>
    <%= react_component('RegistryItems', {registry_id: @registry.id, items: @registry.projects.first.item_types}) %>
  </div>
</div>

<div class="test">
  <div class="c-breaker">
      <div class="c-subhed">
        <div class="center">
        <svg class="c-icon c-icon--large mb"><use xlink:href="#icon-champagne"></use></svg>
        </div>
        <h3 class="c-subhed__info">
          Thank you
        </h3>
        <h2 class="c-subhed__text">
          Your gift has made a difference.
        </h2>
      </div>
  </div>
</div>
