<header class="c-hero section" style='background-image: url(<%=  @registry.banner_image.url ? "#{@registry.banner_image.url}" : "#{image_tag @registry.banner_img}"%>);'>
  <div class="c-hero__container">
    <h1 class="c-hero__couple"><%= @registry.name %></h1>
    <span class="c-hero__date">— May 14, 2016 —</span>
  </div>
</header>
